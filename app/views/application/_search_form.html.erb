<%= form_tag(ideas_path, class:'global-search', method:'GET') do %>
            <div class="input-inline">
              <label class="input-group__label sr-only" for="global-search-input">Looking for inspiration?</label>
              <div class="input-row">
              <%= text_field_tag('q', nil, id: 'global-search-input', class:'input-row__field') %>
                <%= button_tag(label, class: 'btn btn--inline btn--primary') %>
                
              </div>
            </div>
            <% end %>