let comment = {
  'datetime': '<%= @comment.created_at.strftime('%FT%R') %>',
  'when': '<%= time_ago_in_words(@comment.created_at) %>',
  'commenter': '<%= @comment.user.name%> ',
  'body': '<%= @comment.body%>'

}
Comments.displayComment(comment);